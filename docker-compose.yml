services:
  opsbridge:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/opsbridge.db
    volumes:
      - opsbridge-data:/app/data

volumes:
  opsbridge-data:
